
我使用手机录的弹奏钢琴的声音，我想实现识别里面的声音，然后生成对应的声音的播放动画图表，图表上需要看到对应的按键音名，图表：x轴是时间、Y轴是最大音量，那么每个柱子的宽度表示这个音名的声音持续时间长度，不同音名使用不同的颜色标注，同一个音名音量的高低也使用对应颜色同色系的深浅渐变区分。

请使用前端框架实现识别和生成对应的图表，需要实现加载指定文件夹，自动识别到文件夹中的文件，可以控制每个文件去生成对应的图表，还要有存储历史生成记录的功能，历史生成的文件可以恢复查看或者删除，历史记录较多时，还可以一键清空。

让这个音频同时播放，即需要添加音频播放相关的控件

音频播放那个位置要有一个指针，就是这个指针要与图表的位置协同，就是随着音乐的响起，一直有一个指针往前走，然后指针对应的位置可以看到对应的图表位置（特别注意，图表可能会放大缩小看细节或全局，那么需要做好适配）

另外，我希望支持快捷键和自定义，播放速度、向前跳跃、向后回退、暂停、播放等等，其中例如播放速度1表示正常的话，那么可以对播放进行控制例如：梯度是0.2，A键每按下一次减少梯度，D键每按下一次增加梯度，S键可以切换速度1.0和调整之后的速度，Z键可以想后回退5s，X键可以向前跳跃5s，播放或者暂停可以使用空格键切换。这个例子中，有很多关键信息，首先按键可以自定义，梯度可以自定义，需要你实现对应的功能和前端，还有这种快捷键及相关设定的存储。


我最终是要使用tauri构建成一个小应用程序分发。

http://localhost:5173


请查看我的chart_echarts.py和notes_chart.html，我要做的图类似这样的，你现在的图识别到的内容很少


优化：

1、当使用快捷键操作了播放速度之后，播放器控件中依然显示1.00的速度

2、音量图标点击之后，可以切换静音和设定好的原音量
